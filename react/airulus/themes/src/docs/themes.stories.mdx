import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Airulus/themes" />

Mange airulus theme configuration files.

Components ui theme is managed by theme variables. Those should be provided by
theme files, which are applied below given namespace.